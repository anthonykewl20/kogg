{
  "schemaVersion": 1,
  "checks": [
    { "id": "core.runtime", "owner": "kogg-core", "description": "Kogg runtime and platform information is available." },
    { "id": "core.browser-auth", "owner": "kogg-core", "description": "Browser authentication configuration is internally consistent." },
    { "id": "kernel.health", "owner": "kogg-kernel", "description": "The embedded Ranex kernel answers its health request." },
    { "id": "kernel.journal", "owner": "kogg-kernel", "description": "The Ranex evidence journal verifies successfully." },
    { "id": "marketplace.configuration", "owner": "kogg-marketplace", "description": "The exclusive Kogg registry configuration is valid." },
    { "id": "marketplace.registry", "owner": "kogg-marketplace", "description": "The configured signed registry is reachable and returns verifiable manifests." },
    { "id": "marketplace.installed", "owner": "kogg-marketplace", "description": "Installed marketplace package pointers are readable." },
    { "id": "providers.registry", "owner": "kogg-providers", "description": "The provider registry can enumerate configured adapters." },
    { "id": "providers.credentials", "owner": "kogg-providers", "description": "Credential metadata is readable without exposing credential contents." },
    { "id": "projects.registry", "owner": "kogg-projects", "description": "The project registry schema, integrity, foreign keys, and storage permissions are valid." },
    { "id": "projects.repositories", "owner": "kogg-projects", "description": "Registered repository availability can be evaluated without exposing paths." },
    { "id": "projects.restoration", "owner": "kogg-projects", "description": "The active project and pending switch state are internally consistent." },
    { "id": "projects.processes", "owner": "kogg-projects", "description": "No Kogg-owned repository probe is stalled, hidden, orphaned, or residual." },
    { "id": "operations.registry", "owner": "kogg-operations", "description": "The operation registry schema, integrity, foreign keys, and permissions are valid." },
    { "id": "operations.recovery", "owner": "kogg-operations", "description": "Startup reconciliation completed without contradictory operation state." },
    { "id": "operations.processes", "owner": "kogg-operations", "description": "No Kogg-owned process is hidden, stalled, orphaned, unverified, or residual." },
    { "id": "operations.cleanup", "owner": "kogg-operations", "description": "Every terminal operation and owned process has proved cleanup." },
    { "id": "operations.admission", "owner": "kogg-operations", "description": "Operation admission agrees with recovery and cleanup state." },
    { "id": "operations.projection", "owner": "kogg-operations", "description": "The disposable operations projection schema, event chain, lifecycle, and storage integrity are valid." },
    { "id": "operations.owners", "owner": "kogg-operations", "description": "Compiled owner adapters, schemas, epochs, cursors, and freshness are verified." },
    { "id": "operations.correlations", "owner": "kogg-operations", "description": "Owner-event causal references have no gap, conflict, rewind, or cycle." },
    { "id": "operations.timeline", "owner": "kogg-operations", "description": "The safe correlated timeline is complete and deterministically ordered." },
    { "id": "operations.stream", "owner": "kogg-operations", "description": "Authenticated projection streaming, resume, bounds, backpressure, and resync are healthy." },
    { "id": "operations.metrics", "owner": "kogg-operations", "description": "Closed local metrics preserve monotonic epochs, fixed labels, and bounded cardinality." },
    { "id": "operations.support", "owner": "kogg-operations", "description": "Private support export remains bounded, redacted, checksummed, and local." },
    { "id": "operations.actions", "owner": "kogg-operations", "description": "Operations actions are revalidated and routed to authoritative owners without optimistic mutation." },
    { "id": "operations.source-maps", "owner": "kogg-operations", "description": "Operations browser, backend, Electron, and projection code remains debugger-reachable through source maps." },
    { "id": "tasks.registry", "owner": "kogg-tasks", "description": "The task registry schema, integrity, permissions, immutable triggers, and writer state are valid." },
    { "id": "tasks.revisions", "owner": "kogg-tasks", "description": "Task specification revisions and canonical digests verify successfully." },
    { "id": "tasks.bindings", "owner": "kogg-tasks", "description": "Task repository bindings remain current and available." },
    { "id": "tasks.approvals", "owner": "kogg-tasks", "description": "Current task approvals match immutable frozen revisions." }
  ]
}
