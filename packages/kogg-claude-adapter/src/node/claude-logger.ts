import type { ClaudeSafeCode } from '../common/claude-protocol';
// diagnostic-coverage: claude.artifact, claude.authority, claude.legal, claude.confinement, claude.credentials, claude.processes, claude.cleanup, claude.recovery, claude.settings, claude.protocol
type Fields = { 'legal.verify.success': { signingKeyId: string }; 'legal.verify.failure': { safeCode: ClaudeSafeCode }; 'artifact.verify.started': { signingKeyId: string }; 'artifact.verify.completed': { signingKeyId: string; fileCount: number }; 'artifact.verify.failed': { safeCode: ClaudeSafeCode }; 'authority.verify.started': { artifactManifestDigest: string }; 'authority.verify.completed': { artifactManifestDigest: string; executionProfileDigest: string }; 'authority.verify.failed': { artifactManifestDigest: string; safeCode: ClaudeSafeCode }; 'attempt.authorize.requested': { attemptId: string }; 'attempt.authorize.completed': { attemptId: string; authorityDigest: string }; 'attempt.authorize.failed': { attemptId: string; safeCode: ClaudeSafeCode }; 'session.create.requested': { attemptId: string; artifactManifestDigest: string }; 'session.create.completed': { attemptId: string; resourceId: string }; 'session.create.failed': { attemptId: string; safeCode: ClaudeSafeCode }; 'protocol.initialize': { attemptId: string; outcome: string }; 'protocol.progress': { attemptId: string; progressClass: string }; 'protocol.result': { attemptId: string; outcome: string }; 'protocol.failure': { attemptId: string; safeCode: ClaudeSafeCode }; 'protocol.phase.changed': { attemptId: string; phase: string }; 'permission.requested': { attemptId: string; requestId: string; toolId: string }; 'permission.decided': { attemptId: string; requestId: string; decision: string }; 'permission.failed': { attemptId: string; requestId: string; safeCode: ClaudeSafeCode }; 'broker.reservation.requested': { attemptId: string; provider: string; model: string }; 'broker.reservation.completed': { attemptId: string; provider: string; model: string }; 'broker.reservation.failed': { attemptId: string; safeCode: ClaudeSafeCode }; 'broker.activation.requested': { attemptId: string; processId: string }; 'broker.activation.completed': { attemptId: string; processId: string }; 'broker.activation.failed': { attemptId: string; processId: string; safeCode: ClaudeSafeCode }; 'broker.revoked': { attemptId: string }; 'broker.revoke.failed': { attemptId: string; safeCode: ClaudeSafeCode }; 'process.reserve.requested': { attemptId: string }; 'process.reserve.completed': { attemptId: string }; 'process.registered': { attemptId: string; processId: string }; 'process.spawn.completed': { attemptId: string; processId: string }; 'process.signal.completed': { attemptId: string; signalClass: string }; 'process.cleanup.requested': { attemptId: string }; 'process.cleanup.completed': { attemptId: string; residualCount: number }; 'process.cleanup.failed': { attemptId: string; safeCode: ClaudeSafeCode }; 'process.failure': { attemptId: string; safeCode: ClaudeSafeCode }; 'recovery.started': { recoveryBacklog: number }; 'recovery.completed': { processCount: number; residualCount: number; recoveryBacklog: number }; 'recovery.failed': { recoveryBacklog: number; residualCount: number; safeCode: ClaudeSafeCode }; 'diagnostics.failure': { errorType: string } };
const ALLOWED: { [K in keyof Fields]: readonly (keyof Fields[K])[] } = { 'legal.verify.success': ['signingKeyId'], 'legal.verify.failure': ['safeCode'], 'artifact.verify.started': ['signingKeyId'], 'artifact.verify.completed': ['signingKeyId','fileCount'], 'artifact.verify.failed': ['safeCode'], 'authority.verify.started': ['artifactManifestDigest'], 'authority.verify.completed': ['artifactManifestDigest','executionProfileDigest'], 'authority.verify.failed': ['artifactManifestDigest','safeCode'], 'attempt.authorize.requested': ['attemptId'], 'attempt.authorize.completed': ['attemptId','authorityDigest'], 'attempt.authorize.failed': ['attemptId','safeCode'], 'session.create.requested': ['attemptId','artifactManifestDigest'], 'session.create.completed': ['attemptId','resourceId'], 'session.create.failed': ['attemptId','safeCode'], 'protocol.initialize': ['attemptId','outcome'], 'protocol.progress': ['attemptId','progressClass'], 'protocol.result': ['attemptId','outcome'], 'protocol.failure': ['attemptId','safeCode'], 'protocol.phase.changed': ['attemptId','phase'], 'permission.requested': ['attemptId','requestId','toolId'], 'permission.decided': ['attemptId','requestId','decision'], 'permission.failed': ['attemptId','requestId','safeCode'], 'broker.reservation.requested': ['attemptId','provider','model'], 'broker.reservation.completed': ['attemptId','provider','model'], 'broker.reservation.failed': ['attemptId','safeCode'], 'broker.activation.requested': ['attemptId','processId'], 'broker.activation.completed': ['attemptId','processId'], 'broker.activation.failed': ['attemptId','processId','safeCode'], 'broker.revoked': ['attemptId'], 'broker.revoke.failed': ['attemptId','safeCode'], 'process.reserve.requested': ['attemptId'], 'process.reserve.completed': ['attemptId'], 'process.registered': ['attemptId','processId'], 'process.spawn.completed': ['attemptId','processId'], 'process.signal.completed': ['attemptId','signalClass'], 'process.cleanup.requested': ['attemptId'], 'process.cleanup.completed': ['attemptId','residualCount'], 'process.cleanup.failed': ['attemptId','safeCode'], 'process.failure': ['attemptId','safeCode'], 'recovery.started': ['recoveryBacklog'], 'recovery.completed': ['processCount','residualCount','recoveryBacklog'], 'recovery.failed': ['recoveryBacklog','residualCount','safeCode'], 'diagnostics.failure': ['errorType'] }; let violations = 0;
export function claudeLog<K extends keyof Fields>(event: K, fields: Fields[K]): void { const record = fields as Record<string, unknown>; if (Object.keys(record).some(key => !(ALLOWED[event] as readonly string[]).includes(key)) || Object.entries(record).some(([key, value]) => ['processCount','residualCount','recoveryBacklog','fileCount'].includes(key) ? typeof value !== 'number' || !Number.isSafeInteger(value) || value < 0 : typeof value !== 'string' || value.length > 128)) { violations++; console.error('[kogg:claude:artifact] logging.schema.violation', { event }); return; } const component = event.startsWith('recovery.') ? 'recovery' : event.startsWith('protocol.') ? 'protocol' : event.startsWith('permission.') ? 'permission' : event.startsWith('broker.') ? 'broker' : event.startsWith('process.') ? 'process' : event.startsWith('authority.') || event.startsWith('attempt.') || event.startsWith('session.') ? 'authority' : 'artifact'; console[event.endsWith('failure') || event.endsWith('failed') ? 'error' : 'info'](`[kogg:claude:${component}] ${event}`, fields); }
export function claudeLoggingDiagnostics(): { schemaCount: number; violationCount: number } { return { schemaCount: Object.keys(ALLOWED).length, violationCount: violations }; }
